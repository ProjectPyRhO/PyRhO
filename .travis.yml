language: python
python:
#  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
# command to install dependencies
before_install:
    - sudo apt-get update
    # TODO: Move scipy/numpy to anaconda installation
    # https://gist.github.com/dan-blanchard/7045057
    - sudo apt-get python-dev build-essential build-dep python-scipy
    # gcc gfortran liblapack-dev libblas-dev libatlas-base-dev libatlas-dev
    - pip install pytest pytest-cov pytest-pep8
install:
    - pip install cython
    - pip install .
    - pip install -r requirements.txt
#script: python tests/test_utilities.py
script: py.test
branches:
  only:
    #- master
    - dev
